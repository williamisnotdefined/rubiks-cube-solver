pydantic>=2.13.4
pytest>=9.1.1

# Optional for --hf-repo dataset conversion:
# huggingface_hub>=0.24

# Local YOLO training/export dependencies are installed by:
# npm run scan:tile-yolo-install-deps
